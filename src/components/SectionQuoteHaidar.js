import React from 'react'

import Section from '../core/Section'
import Quote from '../core/Quote'

const SectionQuoteHaidar = props => {
  return (
    <Section>
      <Quote text="It’s time for education and technology to be developed and advanced together." />
    </Section>
  )
}

export default SectionQuoteHaidar
